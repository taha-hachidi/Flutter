import 'package:flutter/material.dart';

class cardview extends StatelessWidget {
  const cardview({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child:
    Image.asset("images/MapImg.jpg"));
  }
}